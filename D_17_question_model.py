class Question:
  def __init__(self,q_text,q_answer):
    # now nobody knows what type of variable 
    # would be passed to the class via varialbes q_text and q_answer
    self.text = q_text
    self.answer = q_answer

